@model IEnumerable<WaySpot.Models.Hold>

@{
    ViewBag.Title = "Manage Reservations";
}

<h2>Manage Reservations</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Person)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoldDateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comments)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Person)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoldDateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comments)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HoldID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HoldID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HoldID })
        </td>
    </tr>
}

</table>
